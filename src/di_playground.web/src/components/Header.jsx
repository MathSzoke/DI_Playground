import React from "react";
import { Text } from "@fluentui/react-components";

export default function Header() {
    return (
        <Text size={800} weight="semibold">
            Visual Dependency Injection (DI) in .NET
        </Text>
    );
}
